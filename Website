<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Team Cryptography Project</title>
    <style>
        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background-color: #0e0e0e;
            color: white;
            margin: 0;
            padding: 0;
            line-height: 1.6;
        }
        header {
            background-color: #6a0dad;
            padding: 30px 20px;
            text-align: center;
            box-shadow: 0 4px 10px rgba(0,0,0,0.5);
        }
        header h1 {
            margin: 0;
            font-size: 2.5em;
        }
        .container {
            padding: 20px;
        }
        .team {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(220px, 1fr));
            gap: 20px;
            margin-bottom: 40px;
        }
        .member {
            background: #1f1f1f;
            padding: 20px;
            border-radius: 12px;
            box-shadow: 0 4px 10px rgba(0,0,0,0.3);
            text-align: center;
        }
        textarea {
            width: 100%;
            height: 100px;
            background-color: #2a2a2a;
            color: white;
            border: none;
            padding: 10px;
            border-radius: 8px;
            margin-bottom: 10px;
            resize: none;
        }
        button {
            padding: 10px 20px;
            background-color: #6a0dad;
            border: none;
            border-radius: 8px;
            color: white;
            cursor: pointer;
            font-size: 1em;
            margin-top: 10px;
        }
        button:hover {
            background-color: #520c9e;
        }
        .output {
            background-color: #1a1a1a;
            padding: 10px;
            border-radius: 8px;
            margin-top: 10px;
            white-space: pre-wrap;
        }
    </style>
</head>
<body>
    <header>
        <h1>Cryptography Project</h1>
        <p>Encrypt. Decrypt. Learn.</p>
    </header>
    <div class="container">
        <h2>👥 Meet Our Team</h2>
        <div class="team">
            <div class="member">
                <h3>Norman</h3>
                <p><strong>Frontend Developer</strong></p>
                <p>Developed the core of the website and implemented the encryption/decryption logic.</p>
            </div>
            <div class="member">
                <h3>Abu</h3>
                <p><strong>Algorithm Developer</strong></p>
                <p>Researched and implemented various cryptographic algorithms.</p>
            </div>
            <div class="member">
                <h3>Samandar</h3>
                <p><strong>UI/UX Designer</strong></p>
                <p>Designed the layout and improved user interaction with educational tools.</p>
            </div>
            <div class="member">
                <h3>Khan</h3>
                <p><strong>Documentation & Testing Lead</strong></p>
                <p>Managed testing, red-team feedback, and project documentation.</p>
            </div>
        </div>

        <h2>🛠 Try It Yourself</h2>
        <textarea id="inputText" placeholder="Type text to encrypt/decrypt..."></textarea>
        <button onclick="simpleEncrypt()">Encrypt (Caesar Cipher)</button>
        <button onclick="simpleDecrypt()">Decrypt (Caesar Cipher)</button>
        <div class="output" id="outputText"></div>
    </div>

    <script>
        function simpleEncrypt() {
            const input = document.getElementById('inputText').value;
            let result = '';
            for (let i = 0; i < input.length; i++) {
                const charCode = input.charCodeAt(i);
                result += String.fromCharCode(charCode + 3);
            }
            document.getElementById('outputText').innerText = result;
        }

        function simpleDecrypt() {
            const input = document.getElementById('inputText').value;
            let result = '';
            for (let i = 0; i < input.length; i++) {
                const charCode = input.charCodeAt(i);
                result += String.fromCharCode(charCode - 3);
            }
            document.getElementById('outputText').innerText = result;
        }
    </script>
</body>
</html>
